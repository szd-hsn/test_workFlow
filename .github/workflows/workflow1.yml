name: Workflow 1

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Create artifact file
      run: |
        echo "param1=value1" >> params.txt
        echo "param2=value2" >> params.txt
    - name: Archive artifact
      uses: actions/upload-artifact@v2
      with:
        name: my-artifact
        path: params.txt
