name: Workflow 2

on:
  workflow_run:
    workflows: ["Workflow 1"]
    types:
      - completed

jobs:
  use-data:
    runs-on: ubuntu-latest
    needs: workflow1.build
    steps:
      - name: Get data
        run: |
          echo ${{ needs.workflow1.build.outputs.var1 }}
          echo ${{ needs.workflow1.build.outputs.var2 }}
          echo ${{ needs.workflow1.build.outputs.var3 }}
